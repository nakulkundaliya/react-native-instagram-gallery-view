/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
   AppRegistry,
} from 'react-native';

import InstagramGalleryView from './src/App'

AppRegistry.registerComponent('InstagramGalleryView', () => InstagramGalleryView);
